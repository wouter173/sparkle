{
  "name": "bot",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "npm run build && node out",
    "dev": "ts-node-dev --respawn --rs src",
    "build": "tsc"
  },
  "dependencies": {
    "discord.js": "^14.0.3",
    "dotenv": "^16.0.1",
    "db": "*"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0"
  }
}
