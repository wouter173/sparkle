{
  "name": "db",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "prisma generate && tsc",
    "migrate": "prisma migrate dev && tsc",
    "migrate:reset": "prisma migrate reset && tsc"
  },
  "dependencies": {
    "@prisma/client": "^4.1.0"
  },
  "devDependencies": {
    "prisma": "^4.1.0",
    "typescript": "^4.7.4"
  }
}
